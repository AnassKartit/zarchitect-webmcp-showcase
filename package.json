{
  "name": "zarchitect-webmcp-showcase",
  "version": "1.0.0",
  "private": false,
  "type": "module",
  "description": "A self-contained WebMCP collaboration showcase for ZArchitect.",
  "license": "MIT",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check src/app.js && node --check src/scene.js && node --check src/webmcp.js && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}

